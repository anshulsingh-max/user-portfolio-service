# --- Web framework ---
Django==5.2.14                      # 5.2 LTS — supports Python 3.10–3.14
djangorestframework==3.17.1
django-cors-headers==4.9.0
django-extensions==4.1
django-grappelli==5.0.0              # 4.x+ required for Django 5.x
django-log-request-id==2.1.2
django-redis==6.0.0
django-simple-history==3.11.0
django-health-check==4.4.1
django-prometheus==2.4.1

# --- API docs (swagger) ---
drf-yasg==1.21.15

# --- Database driver (psycopg3) ---
# Django 4.2+ auto-detects psycopg3 behind the existing
# 'django.db.backends.postgresql' engine; no settings change needed.
psycopg[binary]==3.3.4

# --- Celery + scheduler + result store ---
celery==5.6.3
kombu==5.6.2                         # imported directly in configurations/base.py
django-celery-beat==2.9.0
django-celery-results==2.6.0
redis==7.4.0

# --- AWS / HTTP ---
boto3==1.37.38
# requests 2.32.5 carries CVE-2026-25645 (extract_zipped_paths).
# Verified unused in this repo + bw-essentials-core. Pin held back
# until bw-essentials-core==0.1.61 (requests~=2.32.3) releases a
# new floor that allows requests>=2.33.
requests==2.32.5
httpx==0.28.1

# --- App-runtime utilities ---
gunicorn==26.0.0
gevent==26.5.0                       # cp314 wheels + greenlet>=3.2
pandas==3.0.3
sentry-sdk==2.60.0
pymsteams==0.2.5
python-dotenv==1.2.2
PyYAML==6.0.3

# --- Internal libraries ---
bw-essentials-core==0.1.61           # private package; pinned by convention

# --- Lint / test ---
pylint==4.0.5
astroid==4.0.4
coverage==7.14.0
